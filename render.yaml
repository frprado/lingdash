services:
  - type: web
    name: lingdash
    runtime: python
    rootDir: pipeline
    buildCommand: pip install -r ../requirements.txt
    startCommand: gunicorn dashboard:server
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
