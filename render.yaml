services:
  - type: web
    name: lingdash
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn pipeline.dashboard:server
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
